extends layout

block content
  h1 #{vote.question}
  form(action="/vote")
    each alternative in vote.alternatives
      p
        input(type="radio", name="alternatives", value=alternative.title) |alternative.title
        label.radio.inline #{alternative.title}
     input(type="submit", value="Vote!")
